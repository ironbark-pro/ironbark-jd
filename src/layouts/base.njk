<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }}{% else %}Current Openings{% endif %} | Ironbark</title>
    <link rel="icon" href="/assets/favicon-ib.png">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            background: #3C5D5C;
            color: #FFFFFF;
            font-family: Arial, Helvetica, sans-serif;
            line-height: 1.6;
            min-height: 100vh;
        }
        .logo {
            text-align: center;
            padding: 40px 20px 20px;
        }
        .logo img {
            max-width: 300px;
            height: auto;
        }
        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 40px 20px 80px;
        }
        header {
            text-align: center;
            margin-bottom: 50px;
        }
        header h1 {
            font-size: 30px;
            font-weight: 400;
            font-family: "Arial Narrow", Arial, sans-serif;
            color: #FFFFFF;
            margin-bottom: 12px;
        }
        header p {
            font-size: 16px;
            color: rgba(255,255,255,0.8);
            max-width: 700px;
            margin: 0 auto;
        }
        .back-link {
            color: #D4A63A;
            text-decoration: none;
            font-size: 14px;
            display: inline-block;
            margin-bottom: 30px;
        }
        .back-link:hover { text-decoration: underline; }
        .jobs-list {
            display: grid;
            gap: 24px;
        }
        .job-card {
            background: rgba(255,255,255,0.08);
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 8px;
            padding: 28px;
            transition: all 0.3s ease;
            cursor: pointer;
        }
        .job-card:hover {
            background: rgba(255,255,255,0.12);
            border-color: #D4A63A;
            transform: translateY(-2px);
        }
        .job-card h2 {
            font-size: 24px;
            color: #D4A63A;
            font-weight: 600;
            margin-bottom: 10px;
        }
        .job-meta {
            display: flex;
            gap: 16px;
            margin-bottom: 14px;
            font-size: 14px;
            color: rgba(255,255,255,0.7);
        }
        .job-card p {
            color: rgba(255,255,255,0.9);
            font-size: 15px;
            line-height: 1.6;
            margin-bottom: 18px;
        }
        .job-link {
            color: #D4A63A;
            text-decoration: none;
            font-size: 14px;
            font-weight: 600;
        }
        .job-link:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <div style="max-width:900px; margin:0 auto 6px; padding:6px 0; text-align:center;">
        <a href="/" style="display:inline-block;">
            <img src="/assets/ironbark-logo.png" alt="IRONBARK Executive Search" style="max-width:300px;">
        </a>
    </div>

    <div class="container">
        {{ content | safe }}
    </div>

    <div style="text-align:center; padding:40px 20px; border-top:1px solid rgba(255,255,255,0.1);">
        <a href="https://ironbark.pro" style="color:#D4A63A; text-decoration:none; font-size:14px;">← Back to Ironbark</a>
    </div>
</body>
</html>
