# Ironbark Jobs Portal

Автоматизированная система публикации вакансий с использованием Eleventy, Decap CMS и Netlify.

## 🎯 Что делает этот проект

Система автоматически публикует вакансии на сайте **ironbark-jobs.com**:

- HR создаёт вакансию в админке (браузер)
- Eleventy генерирует HTML-страницу автоматически
- Netlify публикует на сайт без участия разработчиков
- Кандидаты подают заявки через форму
- Резюме сохраняются в Netlify Forms

---

## 📁 Архитектура проекта

```
ironbark-jd/
├── jobs/                  # Вакансии (Markdown файлы)
│   ├── lead-engineer-hvac-systems.md
│   ├── mechanical-engineering-manager.md
│   └── senior-manager-engineering-management-platform.md
│
├── public/                # Статические ресурсы
│   ├── admin/             # Decap CMS (админка)
│   │   ├── config.yml     # Конфигурация CMS
│   │   ├── index.html     # Точка входа в админку
│   │   └── published.html # Страница после публикации
│   ├── assets/            # Логотипы и изображения
│   │   ├── ironbark-logo.png       # Светлый логотип (для темного фона)
│   │   └── ironbark-logo-dark.png  # Темный логотип (для светлого фона)
│   └── _redirects         # Правила редиректов Netlify
│
├── src/                   # Исходники сайта
│   ├── layouts/           # Шаблоны страниц
│   │   ├── base.njk       # Layout главной страницы (светлая тема)
│   │   ├── job.njk        # Layout страницы вакансии (темная тема)
│   │   ├── footer.html    # Подвал сайта
│   │   └── header.html    # Шапка сайта
│   ├── pages/             # HTML-страницы
│   │   ├── index.njk      # Главная страница (список вакансий)
│   │   ├── jobs.njk       # Альтернативный список
│   │   ├── hire.html      # Страница процесса найма
│   │   └── success.html   # Страница подтверждения заявки
│   └── styles/            # CSS стили
│       └── main.css       # Глобальные стили
│
├── .eleventy.js           # Конфигурация Eleventy
├── netlify.toml           # Конфигурация деплоя Netlify
├── package.json           # Зависимости Node.js
└── README.md              # Этот файл
```

## 🔧 Функции файлов

### Корневые конфигурационные файлы
| Файл | Функция |
|------|---------|
| `.eleventy.js` | Главная конфигурация Eleventy: `input: "."`, `output: "_site"`, коллекция `jobs` |
| `netlify.toml` | Настройки деплоя: `build.command = "npm run build"`, `build.publish = "_site"` |
| `package.json` | Зависимости проекта и npm-скрипты |

### jobs/ — Вакансии
Каждый `.md` файл содержит:
```yaml
---
layout: "job.njk"
permalink: "/jobs/{{ title | slugify }}.html"
title: "Lead Engineer HVAC Systems"
location: "Austin, TX"
company: "Global Engineering Corp"
responsibilities: "..."
requirements: "..."
conditions: "..."
---
```
**Управление:** через Decap CMS в `ironbark-jobs.com/admin/`

### public/admin/ — Административная панель
| Файл | Функция |
|------|---------|
| `config.yml` | Конфигурация Decap CMS: коллекция `jobs`, поля формы, `site_url` |
| `index.html` | Точка входа в админку |
| `published.html` | Показывает ссылку на опубликованную вакансию |

### public/assets/ — Ресурсы
| Файл | Использование |
|------|---------------|
| `ironbark-logo.png` | Светлый логотип (на темном фоне страниц вакансий) |
| `ironbark-logo-dark.png` | Темный логотип (на светлом фоне главной страницы) |
| `favicon-ib.png` | Иконка сайта |

### src/layouts/ — Шаблоны
| Файл | Функция |
|------|---------|
| `base.njk` | Layout главной страницы: светлая тема, список вакансий, темный логотип |
| `job.njk` | Layout вакансии: темная тема (`#3C5D5C`), светлый логотип, форма заявки |
| `footer.html` | Подвал сайта |
| `header.html` | Шапка сайта |

### src/pages/ — Страницы
| Файл | Функция |
|------|---------|
| `index.njk` | Главная страница `/` — отображает список вакансий из коллекции `jobs` |
| `success.html` | Страница подтверждения после отправки формы |
| `hire.html` | Информация о процессе найма |

---

## 🔄 Как работает система

### Workflow публикации вакансии
1. **HR заходит в админку:** `ironbark-jobs.com/admin/`
2. **Создает новую вакансию:** заполняет форму (title, location, responsibilities, requirements, conditions)
3. **Публикует (Save):** Decap CMS создает commit в GitHub → `jobs/new-job.md`
4. **GitHub Webhook → Netlify:** автоматический деплой
5. **Eleventy генерирует:**
   - `_site/jobs/new-job.html` (по шаблону `job.njk`)
   - Обновляет `_site/index.html` (добавляет вакансию в список)
6. **Netlify публикует:** `_site/` → `ironbark-jobs.com`
7. **HR видит ссылку:** в админке появляется `ironbark-jobs.com/jobs/new-job.html`

### Workflow подачи заявки
1. **Кандидат заходит:** `ironbark-jobs.com/jobs/lead-engineer-hvac-systems.html`
2. **Заполняет форму:** Name, Email, LinkedIn, загружает резюме
3. **Нажимает "Submit Application"**
4. **Netlify Forms обрабатывает:**
   - Сохраняет данные в Netlify Dashboard
   - Отправляет email-уведомление (если настроено)
   - Редиректит на `success.html`
5. **HR проверяет заявки:** `app.netlify.com` → Forms → job-application

---

## 📤 Где хранятся резюме

Резюме попадают в **Netlify Forms**.

### Как посмотреть заявки:
1. Зайдите: **app.netlify.com**
2. Выберите сайт: **ironbark-jobs.com**
3. Перейдите: **Forms → job-application**
4. Увидите все заявки с:
   - Временной меткой
   - Данными кандидата (имя, email, LinkedIn)
   - Прикрепленными резюме (можно скачать)

### Дополнительные опции:
- **Email-уведомления:** автоматические письма при новой заявке
- **Webhook:** интеграция с Zapier/Slack
- **Экспорт CSV:** скачать все заявки одним файлом

---

## ⚙️ Технологический стек

- **Eleventy (11ty)** — генератор статических сайтов (SSG)
- **Nunjucks (.njk)** — шаблонизатор для HTML
- **Netlify** — хостинг и автоматический деплой
- **Netlify Forms** — обработка форм без backend
- **Decap CMS** — headless CMS (редактирование через браузер)
- **GitHub** — хранилище кода и версионность
- **Git Gateway** — аутентификация для Decap CMS

---

## 🎨 Дизайн-система

### Главная страница (`index.njk` + `base.njk`)
- **Фон:** `#FFFFFF` (белый)
- **Логотип:** темный (`ironbark-logo-dark.png`)
- **Заголовок:** "Open Positions"
- **Подзаголовок:** "Stay ahead of the market: explore new opportunities with our clients long before they appear publicly."
- **Карточки вакансий:** светлая тема
- **Типографика:** `Arial Narrow` (заголовки), `Arial/Sans-serif` (основной текст)

### Страница вакансии (`job.njk`)
- **Фон:** `#3C5D5C` (темно-зеленый)
- **Логотип:** светлый (`ironbark-logo.png`)
- **Текст:** белый (`#FFFFFF`)
- **Секции:**
  - "About the Company"
  - "Role Overview" (responsibilities)
  - "What We're Looking For" (requirements)
  - "What We Offer" (conditions)
- **Форма:** темная с белыми полями ввода
- **Типографика:** `Arial Narrow` (заголовки), `Arial/Sans-serif` (основной текст)

---

## 🚀 Локальная разработка

### Установка зависимостей
```bash
npm install
```

### Запуск локального сервера
```bash
npm run dev
```
Сайт доступен на `http://localhost:8080`

### Сборка для production
```bash
npm run build
```
Результат в папке `_site/`

---

## 🔐 Безопасность

- Админка защищена **Git Gateway** (OAuth через GitHub)
- Формы защищены от спама **reCAPTCHA** (если включено)
- Резюме хранятся на защищенных серверах Netlify
- SSL-сертификат для `ironbark-jobs.com` (HTTPS)

---

## 📝 Важные правила

### ❌ НИКОГДА не используйте:
- "Careers at Ironbark" (искажает позиционирование)
- Layout paths с `layouts/` (только `job.njk`, не `layouts/job.njk`)
- Input/output отличные от `input: "."` и `output: "_site"`

### ✅ ВСЕГДА используйте:
- "Open Positions" на главной
- Clarification: Ironbark — АГЕНТСТВО, размещающее с КЛИЕНТАМИ
- Правильный логотип: светлый на темном, темный на светлом
- `Arial Narrow` для заголовков (профессиональный стиль, без капслока)

---

## 📞 Контакты

- **Live Site:** https://ironbark-jobs.com
- **Admin Panel:** https://ironbark-jobs.com/admin/
- **GitHub:** https://github.com/ironbark-pro/ironbark-jd
- **Netlify Dashboard:** https://app.netlify.com

---

## 📄 Лицензия

Proprietary — все права принадлежат Ironbark Executive Search.
